#!/usr/bin/env bash
# vi:ts=4:sts=4:sw=4:et
# ------------------------------------------------------------------------------
# File is sourced when shell is:
# - login interactive (example: ssh HOST; echo CMD | ssh HOST; su - USER; local (physical) console)
# - login non-interactive (example: ssh HOST < LOCAL_SCRIPT.sh)
# ------------------------------------------------------------------------------

source $HOME/.bashrc
